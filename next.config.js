/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['dkstatics-public.digikala.com' ,'digikala.com' ],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
